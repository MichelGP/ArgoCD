apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wordpress-gp-ingress
  namespace: wordpress-greypixel
  annotations:
    # Cilium supports websocket upgrades automatically,
    # so you don't need nginx-specific proxy annotations.
    ingress.cilium.io/loadbalancer-mode: "shared"
spec:
  ingressClassName: cilium
  rules:
    - host: www.greypixel.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wordpress-gp
                port:
                  number: 80
    - host: greypixel.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wordpress-gp
                port:
                  number: 80
    - host: michelvanderzijden.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wordpress-gp
                port:
                  number: 80
    - host: www.michelvanderzijden.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wordpress-gp
                port:
                  number: 80