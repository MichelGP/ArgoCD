apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: zabbix-mysql
spec:
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 200Gi
