apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql-dev
spec:
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        resources:
          requests:
            storage: 10Gi
